# Lightning instalador

Script para atualizar pacotes dos repositórios Linux e instalar um programa automaticamente.

## Utilização

Execute o comando do seu terminal, no meu caso o zsh:

```shell
sudo zsh light.sh "nome-do-programa"
```

